using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemAssign : MonoBehaviour
{
    // Start is called before the first frame update
    public Item itemItIs;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
